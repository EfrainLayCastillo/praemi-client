class Order {
  int idOrder;
  String status;
  String total;
  int customerId;
  String itemName;

  Order(
      {this.idOrder, this.status, this.total, this.customerId, this.itemName});
}
